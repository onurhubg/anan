pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                // Checkout Timeriddle repository
                git url: 'https://github.com/PeterPorzuczek/TimeRiddle.git'
            }
        }
    }
}
