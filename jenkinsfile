pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Example..'
                // Here you can find the commands that build your application, such as mvn clean install
            }
        }
}
